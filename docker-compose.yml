services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file: .env.local
    volumes:
      - prospector_data:/app/data
    restart: unless-stopped
    init: true
    security_opt:
      - no-new-privileges:true
volumes:
  prospector_data:
