import { NextResponse } from "next/server";import { z } from "zod";import { audit,requireSession } from "../../../../lib/auth";import { db } from "../../../../lib/db";import { syncHubSpotContact } from "../../../../lib/providers";
export async function POST(request:Request){try{const s=await requireSession();const {prospectId,email}=z.object({prospectId:z.string(),email:z.string().email().optional()}).parse(await request.json());const p=db().prepare("SELECT name,role,company,linkedin_url linkedinUrl FROM prospects WHERE id=? AND workspace_id=?").get(prospectId,s.workspaceId) as {name:string;role:string;company:string;linkedinUrl:string}|undefined;if(!p)return NextResponse.json({error:"Prospect not found."},{status:404});const token=process.env.HUBSPOT_ACCESS_TOKEN;if(!token)return NextResponse.json({error:"HUBSPOT_ACCESS_TOKEN is not configured."},{status:503});const result=await syncHubSpotContact(token,{...p,email});audit(s.workspaceId,s.userId,"crm.contact_synced","prospect",prospectId,{provider:"hubspot",providerId:result.id});return NextResponse.json({provider:"hubspot",id:result.id});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Sync failed."},{status:502});}}
